<Window x:Class="WpfApp1.AllStatisticGroup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="AllStatisticGroup" Height="450" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions >
                <RowDefinition Height="25*"/>
                <RowDefinition Height="25*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Выберети Специальность" Grid.Row="0"/>
            <ComboBox
        Width="Auto" 
        Height="25" 
        ItemsSource="{Binding Specials}" 
        SelectedItem="{Binding SelectedSpecial}" 
        DisplayMemberPath="Title"
        Grid.Row="0"
                />

            <Button Width="250" Height="100" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Назад"  Click="Close"/>
        </Grid>

        <ListView Grid.Column="0" Grid.RowSpan="2" Background="Beige" ItemsSource="{Binding Groups}"  >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название группы" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Кол-во Студентов" DisplayMemberBinding="{Binding CountStudent}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
